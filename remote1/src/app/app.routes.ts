import { Routes } from '@angular/router';
import { RemoteCompComponent } from './remote-comp/remote-comp.component';

export const routes: Routes = [
  {
    path: 'remote',
    component: RemoteCompComponent,
  },
];
